# ============================================================
# NUNCA versionar segredos nem lixo do ambiente
# ============================================================

# Segredos (chaves de API) -> NUNCA vao para o git
.env

# Ambiente virtual de Python
.venv/
venv/

# Caches do Python
__pycache__/
*.pyc

# Ficheiro de estado (tokens ja vistos) - opcional
seen.json

# Logs
*.log
carteira.json
posicoes.json
radar.json
watchlist.json
sniper_rapido.json
copy_trade.json
vistos_recentemente.json
bot.pid
# Memoria semanal (dados de execucao, nao codigo) - fica so no aparelho
memoria/
wallet.py.*
gerar_wallet.py

# Chave de sessao do dashboard (assina os cookies de login) - NUNCA versionar
.dashboard_secret
